// @flow
import React from 'react';
import {
	Text,
	View,
} from 'react-native';
import styles from './styles';

type Props = {
	text: string,
}

const Component = (props: Props) => (
	<View>
		<Text></Text>
	</View>
);
