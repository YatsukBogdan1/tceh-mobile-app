import { StyleSheet } from 'react-native';
import styles from '../styles';

export default StyleSheet.create({
	label: {
		...styles.label,
		marginBottom: 50,
	},
	text: {
		...styles.text,
		marginBottom: 30,
	},
	inputContainer: {
		marginBottom: 30,
	},
});
